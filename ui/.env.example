## UI environment (Astro)
#
# Any variable that must be available in the browser must be prefixed with `PUBLIC_`.
#
# Chat API endpoint (preferred): full URL to the API Gateway /chat route.
# Example:
# - https://<api-id>.execute-api.eu-central-1.amazonaws.com/prod/chat
PUBLIC_CHAT_API_URL=

CHECK_PREFLIGHT=1
# if disabled, OPTIONS request is simulated

# Fallback: base URL (without /chat). If set, UI will call `${PUBLIC_CHAT_API_BASE_URL}/chat`.
# Example:
# - https://<api-id>.execute-api.eu-central-1.amazonaws.com/prod
PUBLIC_CHAT_API_BASE_URL=

# PostHog Analytics
# Only used in production (localhost is automatically excluded)
PUBLIC_POSTHOG_KEY=
PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com

