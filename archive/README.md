# Archive

This directory holds deprecated implementation paths kept for historical reference.

- `chat-api-lambda/`: legacy Lambda + OpenSearch chat API
- `infra/`: legacy AWS CLI deployment scripts for the Lambda stack

The active baseline for the project lives at the repo root:

- `ui/`
- `chat-api-service/`
- `ingest/`
- `infra-vps/`
