OPENAI_API_KEY=""
OPENAI_EMBEDDING_MODEL=text-embedding-3-small
OPENAI_EMBEDDING_DIM=1536
AWS_REGION=eu-central-1
# Preferred names (ingest scripts accept both preferred + legacy aliases):
AOSS_ENDPOINT=
AOSS_ITEMS_INDEX=content_items_v1
AOSS_CHUNKS_INDEX=content_chunks_v1

# Legacy aliases (still supported):
OPENSEARCH_ENDPOINT=
OS_INDEX_ITEMS=
OS_INDEX_CHUNKS=
