---
import Layout from '../layouts/Layout.astro';
import NavBar from '../components/NavBar.astro';
import ChatSplitView from '../components/ChatSplitView';

const searchParams = Astro.url.searchParams;
const initialMessage = searchParams.get('message') || '';
---

<Layout title="Chat — Jaan Sokk">
  <NavBar />
  
  <!-- Main container: full viewport height, nav is ~80px (py-5 + content) -->
  <div class="h-screen bg-bg pt-20 overflow-hidden flex flex-col">
    <div class="flex-1 min-h-0 max-w-[1400px] mx-auto px-10 py-8">
      <ChatSplitView client:load initialMessage={initialMessage} />
    </div>
  </div>
</Layout>

